PORT=5000
CORS_ORIGIN=http://localhost:3000

# Change these values before using the login screen.
LOGIN_USERNAME=admin
LOGIN_PASSWORD=change_me

# JWT secret used to sign tokens (change for production).
JWT_SECRET=change_me_too

# Serper API key used for searches (used by POST /api/search)
SERPER_API_KEY=change_me_serper

